// Options page entrypoint. API key, domain allowlist/blocklist, keyboard
// shortcut config, cost disclosure. See architecture.md §10.1, §10.4, §10.9.

console.debug('[pagewise] options page loaded');
